---
name: claude-md-marker-region-is-generated
description: Everything between CLAUDE.md's scrumia:start/end markers is regenerated by scrumia-init Step 5 — a skill must never depend on hand-written prose inside it
metadata:
  type: project
---

`CLAUDE.md` lines between `<!-- scrumia:start -->` and `<!-- scrumia:end -->` are
**generated**. `scrumia-init` Step 5 says *"Replace only what sits between the markers"*
and writes its own template. Any sentence hand-added inside that region survives only
until the next `scrumia-init` run — which is a documented, supported operation
("use it on first installation, or after adding or changing a module").

This repo's `CLAUDE.md:29-30` — *"`plugins/` is the product itself … no implementation
module speaks for it"* — sits **inside** the markers and is **not** in the template.
It is hand-written and destructible.

**Why this trips reviews up:** a plugin skill can be tempted to point at a hand-written
sentence inside the markers as if it were a guaranteed part of every consumer's
`CLAUDE.md`. Two failures at once follow: a re-run of `scrumia-init` deletes the sentence
in *this* repo, and in any fresh consumer install the sentence was never written, so a
distributable module asserts content that `scrumia-core` does not emit.

**How to apply:** before approving any skill that says "read it in `CLAUDE.md`", open
`scrumia-core/skills/scrumia-init/SKILL.md` Step 5 and confirm the template actually emits
that content. If it does not, the honest fixes are to extend the template (making the
statement regenerable) or to condition the pointer ("*if* the composition names …").
Never both assert it and leave the generator silent.
