<!-- generated by scrumia-assemble; do not edit. Rebuild with: scrumia-assemble build -->
# Composed assemblies

Inputs digest: `fa2738076585ec24e9bf732ec881194ddaad50cc8b8408d60df9a0f0364e8c7f`

`scrumia-assemble load` refuses when that digest no longer matches and says to
rebuild; it never recomputes an answer on the spot. A plugin version bump does not
invalidate it — these files hold routing, not prose, so a rewritten guide leaves them
correct. A module that changed shape is caught by `scrumia-assemble check` and by the
missing fragment itself.

## Modules consulted

| Module | Describes itself through | Actions provided |
|---|---|---|
| `scrumia-core` | `scrumia-core-manifest` | 3 |
| `scrumia-design` | `scrumia-design-manifest` | 4 |
| `scrumia-discovery` | `scrumia-discovery-manifest` | 2 |
| `scrumia-github-project` | `scrumia-github-project-manifest` | 6 |
| `scrumia-specs` | `scrumia-specs-manifest` | 4 |
| `scrumia-teams` | `scrumia-teams-manifest` | 4 |

## Run coverage

Coverage counts **module callers**. A person being able to invoke an action does not
remove it from the hole count — it changes the remedy the row offers.

| Action | Kind | Step | Provided by | State |
|---|---|---|---|---|
| `brief/scope-idea` | contribution | Brief | `scrumia-discovery` | hole — no module calls it; a person reaches it by `/scrumia-discovery:scrumia-brainstorm` |
| `brief/state-need` | decision | Brief | — | a person covers it, no tool |
| `scoping/split-into-work` | decision | Scoping | `scrumia-discovery` | hole — no module calls it; a person reaches it by `/scrumia-discovery:scrumia-split` |
| `scoping/write-spec` | contribution | Scoping | `scrumia-specs` | covered — 2 module caller(s) |
| `refinement/refine-ticket` | decision | Refinement | `scrumia-github-project` | hole — no module calls it; a person reaches it by `/scrumia-github-project:refine` |
| `sign-off/approve-ready` | decision | Sign-off | — | a person covers it, no tool |
| `sprint/assemble-batch` | decision | Sprint | `scrumia-teams` | hole — no module calls it; a person reaches it by `/scrumia-teams:sprint` |
| `build/execute-ticket` | decision | Build | `scrumia-github-project` | covered — 1 module caller(s) |
| `build/pick-model` | decision | Build | `scrumia-teams` | covered — 2 module caller(s) |
| `build/move-card` | decision | Build | `scrumia-github-project` | hole — no module calls it, and no entry point |
| `build/find-spec` | contribution | Build | `scrumia-specs` | hole — no module calls it; a person reaches it by `/scrumia-specs:scrumia-specs-find` |
| `build/apply-design-system` | contribution | Build | `scrumia-design` | hole — no module calls it; a person reaches it by `/scrumia-design:scrumia-design-system` |
| `build/sync-design-system` | contribution | Build | `scrumia-design` | hole — no module calls it; a person reaches it by `/scrumia-design:scrumia-design-sync` |
| `build/apply-implementation` | contribution | Build | — | not applicable here — out of the denominator |
| `merge/review-pr` | contribution | Merge | `scrumia-github-project` | hole — no module calls it; a person reaches it by `/scrumia-github-project:review` |
| `merge/approve` | decision | Merge | — | a person covers it, no tool |
| `read/board-status` | contribution | — | `scrumia-github-project` | covered — 2 module caller(s) |
| `read/next-step` | contribution | — | `scrumia-core` | hole — no module calls it; a person reaches it by `/scrumia-core:next` |
| `read/convene-roles` | contribution | — | `scrumia-teams` | hole — no module calls it; a person reaches it by `/scrumia-teams:standup` |
