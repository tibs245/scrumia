# Knowledge placement

**Status**: draft

## In brief

What happens to something an agent just learned. One tree, one destination per fact:
a module when it holds for every project running that module, the project when it holds
for this repository, a feature when it changes what the product does, a ticket when it is
a discussion rather than a rule — and agent memory only for what would not survive being
handed to someone else. Deciding to fix it now is a branch of the same tree, not a
separate question. The tree reminds; it does not block.

## Links

- Implemented by: no App feature. What this feature describes is a skill in
  `scrumia-core`, reachable on its own and cited from the registers where learning
  happens.
- Defers to: `features/business/local-extension/` for what the destinations *are* when
  the answer is a module or local material, and which of them a project may write to.
- Defers to: `features/business/module-authoring/` for what creating a module costs, and
  for the refusal to create one for a single rule — the two refusals meet here from
  opposite sides.
- Defers to: `features/business/github-tracking/` for what an issue carries, how it is
  labelled and where those labels are declared. This feature says a discussion becomes an
  issue; it spells no label.
- Defers to: `features/business/feature-format/` for what belongs in a feature and in
  which file. This feature routes toward a feature; it decides nothing about its shape.
- Defers to: `features/business/dev-flow/` for the two paths a piece of work travels.
  Choosing to fix something now rather than file it is a destination in this tree and a
  path in that one.

## Files present

| File | Read it when |
|---|---|
| `business.md` | Deciding where something learned belongs, what agent memory may hold, and what happens when the destination's slot is empty |
| `qa.md` | Checking this feature's acceptance criteria |
| `CHANGELOG.md` | History of changes to this spec |

No `legal.md`: the tree routes a project's own material and reads nothing personal. No
`security.md`: every destination it names is one the project already writes to by other
means.

