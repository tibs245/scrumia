# Extends map — the home page's extension section

**Status**: draft

## In brief

The `#extends` section of the home page: how a skill declares an extension point, how any
module contributes to it as data, and how the table is computed at the moment it is asked
for. Drawn once, as a diagram of the real mechanism rather than as a description of it.
This is the section that carries the argument the site currently does not make —
`extends` appears twice on the whole home page and nowhere in the reference.

## Links

- Business parent: none. `features/business/modular-composition/index.md` names its own
  mechanism as implemented by no App feature — this feature renders that business
  concept, exactly as `slot-index` does for slots, without being its implementation.
- Authority:
  - `features/business/modular-composition/index.md` — what a register, a directive and
    a contribution are, and the order the table renders in
  - `features/business/local-extension/index.md` — the three locations a contributing
    module may live in, which the diagram has to make visible or deliberately omit
  - `design/identity.md` — the mechanism-over-claim rule this section is most at risk of
    breaking
  - `design/tokens.css` — the vocabulary of values this feature may use
- Neighbouring sections: `features/app/site/slot-index/` answers *what can be replaced*;
  this one answers *what can be added without replacing anything*. Neither restates the
  other, and a reader arriving at one must be able to reach the other.

## Files present

| File | Read it when |
|---|---|
| `business.md` | Checking the value this section brings and why the rule it renders is `modular-composition`'s |
| `ux.md` | Checking what the diagram must make visible, what it may not claim, and the section's reading order |
| `qa.md` | The criteria this section must pass — no-JS, both themes, keyboard, and the claim rule |
| `CHANGELOG.md` | History of changes to this spec |

No `tech.md`: the section is generated by the same build as every other, and introduces
no mechanism of its own. If the diagram turns out to need one, that is the moment the
file earns its place.

